<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.EXECUTE_COMMANDS" />
    <!-- API 35: requires due to sender null -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <!--
      Vendored top.yukonga.miuix.kmp:miuix-blur-android declares minSdk 33; force its
      usage since its blur APIs are only invoked on API 33+ at runtime (guarded in
      the vendored ScrcpyForAndroid Compose code), while this app's own minSdk stays 23.
    -->
    <uses-sdk tools:overrideLibrary="top.yukonga.miuix.kmp.blur" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ananbox"
        tools:targetApi="31">

        <!--        <receiver-->
        <!--            android:name=".BinderReceiver"-->
        <!--            android:enabled="true"-->
        <!--            android:exported="true" />-->

        <provider
            android:name=".StorageProvider"
            android:authorities="com.cyanmint.anbox.documents"
            android:exported="true"
            android:grantUriPermissions="true"
            android:permission="android.permission.MANAGE_DOCUMENTS">
            <intent-filter>
                <action android:name="android.content.action.DOCUMENTS_PROVIDER" />
            </intent-filter>
        </provider>

        <activity
            android:name="io.github.miuzarte.scrcpyforandroid.MainActivity"
            android:configChanges="orientation|keyboardHidden"
            android:exported="true"
            android:resizeableActivity="false"
            android:theme="@style/Theme.Ananbox">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
    </application>

</manifest>